# Third-party notices

This file records the third-party packages currently present in `Cargo.lock`
for release and source-distribution review.

## Rust crates

The current dependency set is from the `windows-rs` project:

- `windows-sys` 0.59.0
- `windows-targets` 0.52.6
- `windows_aarch64_gnullvm` 0.52.6
- `windows_aarch64_msvc` 0.52.6
- `windows_i686_gnu` 0.52.6
- `windows_i686_gnullvm` 0.52.6
- `windows_i686_msvc` 0.52.6
- `windows_x86_64_gnu` 0.52.6
- `windows_x86_64_gnullvm` 0.52.6
- `windows_x86_64_msvc` 0.52.6

The `windows-rs` crates are licensed under `MIT OR Apache-2.0`.
The package metadata for the direct dependency `windows-sys` 0.59.0 and
`windows-targets` 0.52.6 was checked from the local Cargo registry cache.

Project repository:

https://github.com/microsoft/windows-rs